import type { EventObject } from "/types.tsx";

export default function ChartItem(props: EventObject[]) {
  // TODO: shared app_ids should have same color
  // all app_id is the same, all id is the same
  // timestamp, action

  return (
    <div className="chartItem">
    hi
    </div>
  )
}
